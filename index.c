#include <stdio.h>
#define MAX_SIZE 100
int main()
{
    int arr[MAX_SIZE];
    int size, i,;
printf("Enter size of array: ");
    scanf("%d", &size);
printf("Enter elements in array: ");
    for(i=0; i<size; i++)
    {
        scanf("%d", &arr[i]);
    };
for(i=0; i<size; i++)
    {
  found = 1;
            break;}
else
return 0;}
